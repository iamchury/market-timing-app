[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "market-timing-app"
version = "0.1.0"
dependencies = ["streamlit", "pandas", "numpy", "yfinance", "PyYAML", "plotly"]
[tool.pytest.ini_options]
pythonpath = ["src"]
